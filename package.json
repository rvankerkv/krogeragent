{
  "name": "kroger-recipe-agent",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "build:web": "pnpm --filter @kroger/web build",
    "build:api": "pnpm --filter @kroger/api build"
  }
}
